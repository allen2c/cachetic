"""Asynchronous cache backends.

Each module here mirrors its synchronous sibling in ``cachetic.extensions``,
sharing the URL parsing and value format so a cache written by one client is
readable by the other.
"""
