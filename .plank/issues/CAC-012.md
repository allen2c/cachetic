---
id: CAC-012
title: Make `rich` a conditional import
status: done
priority: low
epic: CAC-E03
created: 2026-04-01
updated: 2026-04-01
---

# Make `rich` a conditional import

## Description

`from rich.pretty import pretty_repr` is a top-level import in
`cachetic/__init__.py`. It is only used inside `logger.debug()` calls
guarded by `logger.isEnabledFor(logging.DEBUG)`.

Replace with a conditional import that falls back to `repr()` when
`rich` is not installed, reducing the hard dependency footprint.

### Approach

```python
try:
    from rich.pretty import pretty_repr
except ImportError:
    pretty_repr = repr  # type: ignore[assignment]
```

Or, since `pretty_repr` is only used in debug-guarded blocks, a lazy
import inside those blocks is also acceptable.

## Acceptance Criteria

- [x] `cachetic` importable without `rich` installed
- [x] Debug logging still works (using builtin `repr()`)
- [x] Debug logging gracefully falls back to `repr()` without `rich`
- [x] `rich` was never in `[project.dependencies]` — removed top-level import entirely

## Action Log

### 2026-04-01
- Created ticket from v0.7.0 spec discussion
- Replaced `pretty_repr(x, max_string=40)` with `repr(x)` at all 3 call sites
- Removed `from rich.pretty import pretty_repr` top-level import entirely
- `rich` was only a dev dependency; never needed a conditional fallback — just dropped it. CAC-012 closed.
